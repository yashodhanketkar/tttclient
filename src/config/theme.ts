import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#F00",
    },
    background: {
      default: "#EEE",
      paper: "#FFF",
    },
    text: {
      primary: "#000",
      secondary: "#FFF",
    },
  },
});
